package Tiles;

import javafx.scene.paint.Color;

public class ShipPixel extends Tile {
    public ShipPixel(int i, int j, int tileSize, Color color) {
        super(i, j, tileSize, "Ship", color);
    }
}
